[project]
name = "metabolism-low-dimensional"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cobra>=0.32.1",
    "cvxpy>=1.9.2",
    "matplotlib>=3.11.1",
    "numpy>=1.26",
    "openpyxl>=3.1",
    "scipy>=1.18.1",
    "snakemake>=8.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["metabolism_low_dim"]
